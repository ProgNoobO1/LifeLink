# LifeLink Email Configuration
# For Gmail SMTP: set smtp.host=smtp.gmail.com, smtp.port=587, smtp.auth=true, smtp.starttls=true
# Use an App Password if using Gmail with 2FA.

smtp.host=smtp.gmail.com
smtp.port=587
smtp.auth=true
smtp.starttls=true

# Sender credentials (update these)
mail.from=your-email@gmail.com
mail.password=your-app-password

# Display name shown in inbox
mail.from.name=LifeLink Blood Management
